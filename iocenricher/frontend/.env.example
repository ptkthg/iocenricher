# Opcional — só defina se o backend rodar em domínio diferente do frontend.
# Com o vercel.json na raiz do repo (deploy monorepo), NÃO é necessário.
# Exemplo: VITE_API_URL=https://meu-backend.vercel.app/api
VITE_API_URL=
